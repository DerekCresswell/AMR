
#include <gtest/gtest.h>

#include <AMR\Sets\FiniteSet.hpp>

using namespace AMR::Sets;

TEST(FiniteSetTest, m_in) {

	//Element e_1 = Element(1),
	//	e_2 = Element(2),
	//	e_3 = Element(3);

	//std::vector<Element> v4;
	// FiniteSet fs = FiniteSet(v);

	// EXPECT_TRUE(fs.in(2));

	EXPECT_TRUE(true);
	EXPECT_FALSE(false);
	EXPECT_FALSE(true);

}

TEST(FiniteSetTest, m_otherFunction) {

	EXPECT_TRUE(true);

}